.productItem
  & img
    width: 100%
    height: 160px 
    object-fit: cover

.addButton
  width: 100%
  height: 2.5rem;
  font-size: 1.4vw;
  background-color: var(--jade);
  border-radius: 0 0 .5rem .5rem;
  padding: 0;
  &:hover
    background-color: var(--jade-light);

.orderProducts
  height: 100%